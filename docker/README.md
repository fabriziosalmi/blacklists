# Docker

This directory contains Docker-related files and configurations.

## Pi-hole with Squid

The `pihole-squid` directory contains a `docker-compose.yml` file to run Pi-hole and Squid together.

### Usage

To run the containers, execute the following command:

```bash
docker-compose up -d
```