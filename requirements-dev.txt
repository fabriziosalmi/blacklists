# Development and test dependencies.
# Runtime dependencies are installed by the workflows themselves (tldextract,
# tqdm for the pipeline; matplotlib and pandas for the statistics job).
pytest>=8.0
tldextract>=5.0
tqdm>=4.66

# Used only by scripts/check_whitelist.py, which probes whitelisted domains.
requests>=2.31
rich>=13.0
