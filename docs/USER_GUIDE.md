# User Guide

This guide provides instructions for using the blacklists project.

## Installation

To install the project, execute the following command:

```bash
pip install -r requirements.txt
```

## Usage

To run the project, execute the following command:

```bash
python3 generate.py
```